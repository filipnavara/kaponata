{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch chrome (kaponata-ui)",
            "url": "http://localhost:4200",
            "webRoot": "${workspaceFolder}/src/kaponata-ui"
        }
    ]
}
